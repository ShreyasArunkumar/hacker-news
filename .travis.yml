language: node_js
node_js:
  - 14.2.0
cache: npm

install:
  - yarn install

script:
  - yarn test
  - yarn run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $github_token
  local_dir: build
  on:
    branch: master
